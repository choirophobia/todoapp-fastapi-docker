adding new features like docker and integrate with fastAPI for the CRUD
you can also performed the CRUD through dbeaver/postman

How to start : 
- docker compose up -d  // this is to execute whats inside the docker-compose.yml
- uvicorn app:app --reload // this is to startup the fastapi server so you can access it through your localhost


*please make sure the port of the postgres does not conflicted with your previous project
